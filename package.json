{
  "name": "mosu",
  "version": "0.1.0",
  "description": "mosu! — a free, open-source, browser-based osu!mania beatmap editor.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "graph": "graphify update",
    "graph:serve": "graphify serve .graphify/graph.json"
  },
  "devDependencies": {
    "@sentropic/graphify": "^0.10.0",
    "@types/node": "^20.19.42",
    "jsdom": "^25.0.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
